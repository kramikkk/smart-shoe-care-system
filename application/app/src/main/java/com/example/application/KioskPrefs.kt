package com.example.application

/** Shared preference file name and keys for kiosk state (non-PIN). */
object KioskPrefs {
    const val FILE_NAME = "kiosk_prefs"
    const val KEY_KIOSK_ACTIVE = "kiosk_mode_active"
    const val KEY_LAUNCH_ON_BOOT = "launch_on_boot"
}
